export default function FeeStructures() {
  return (
    <div>
      <h2 className="page-title">Fee Structures</h2>
      <div className="alert alert-light border">
        Define fee structures per grade + term + academic year, generate invoices and reconcile payments. Wire up financeApi here.
      </div>
    </div>
  );
}
