export default function Rankings() {
  return (
    <div>
      <h2 className="page-title">Rankings</h2>
      <div className="alert alert-light border">
        Trigger ranking computation (midterm/endterm) per classroom or whole grade and view positions. Wire up examsApi.rank()/rankings() here.
      </div>
    </div>
  );
}
