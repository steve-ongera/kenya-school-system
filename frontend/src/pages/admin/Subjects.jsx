export default function Subjects() {
  return (
    <div>
      <h2 className="page-title">Subjects</h2>
      <div className="alert alert-light border">
        Manage subjects, papers (e.g. Maths PP1/PP2), compulsory vs optional per grade, and selection rules. Wire up academicsApi.subjects()/gradeSubjects()/selectionRules() here.
      </div>
    </div>
  );
}
