export default function TeacherAllocation() {
  return (
    <div>
      <h2 className="page-title">Teacher Allocation</h2>
      <div className="alert alert-light border">
        Allocate teachers to (subject, classroom) pairs - a teacher can teach the same subject in multiple streams. Wire up teacherApi.allAllocations() here.
      </div>
    </div>
  );
}
