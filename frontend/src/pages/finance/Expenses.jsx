// src/pages/finance/Expenses.jsx
// Finance's own route for the same Expenses page (mirrors how
// Collections/ClassAnalysis/DetailedReport each have an admin/ and
// finance/ copy pointing at the same underlying UI).
export { default } from "../admin/Expenses";