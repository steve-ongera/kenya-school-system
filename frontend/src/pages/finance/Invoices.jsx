export default function Invoices() {
  return (
    <div>
      <h2 className="page-title">Invoices</h2>
      <div className="alert alert-light border">
        Generate and view student invoices. Wire up financeApi.invoices()/generateInvoice() here.
      </div>
    </div>
  );
}
