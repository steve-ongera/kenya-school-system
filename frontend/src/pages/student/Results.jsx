export default function MyResults() {
  return (
    <div>
      <h2 className="page-title">My Results</h2>
      <div className="alert alert-light border">
        Per-term results by subject, with grade letters and class/grade position. Wire up examsApi.results()/rankings() filtered to this student.
      </div>
    </div>
  );
}
