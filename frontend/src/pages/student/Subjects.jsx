export default function MySubjects() {
  return (
    <div>
      <h2 className="page-title">My Subjects</h2>
      <div className="alert alert-light border">
        View (and where allowed, select) subjects for the current academic year, respecting min/max selection rules.
      </div>
    </div>
  );
}
